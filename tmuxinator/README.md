## INSTALATION

This is a program that allows to recreate tmux session configurations

```{shell}
sudo apt update
sudo apt install ruby-full
gem install --user-install tmuxinator
```

After that update the `$PATH` variable (you should know what to do based on the installer cli output).
